
<% if (popup==="not login") { %>
    <div class="d-flex justify-content-center m-5 card-img-overlay" style="max-height: 13%; color: red;">
        <div class="toast toast-demo fade show" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-header">
            <strong class="me-auto">แจ้งเตือน</strong>
            <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body">
                กรุณาเข้าสู่ระบบ
            </div>
        </div>
    </div>
<% } %>

<% if (popup==="user not found") { %>
    <div class="d-flex justify-content-center m-5 card-img-overlay" style="max-height: 13%; color: red;">
        <div class="toast toast-demo fade show" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-header">
            <strong class="me-auto">แจ้งเตือน</strong>
            <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body">
                ไม่พบผู้ใช้งาน กรุณากรอก Username ใหม่
            </div>
        </div>
    </div>
<% } %>

<% if (popup==="password not match") { %>
    <div class="d-flex justify-content-center m-5 card-img-overlay" style="max-height: 13%; color: red;">
        <div class="toast toast-demo fade show" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-header">
            <strong class="me-auto">แจ้งเตือน</strong>
            <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body">
                รหัสผ่านไม่ถูกต้อง กรุณากรอกรหัสผ่านใหม่
            </div>
        </div>
    </div>
<% } %>

<% if (popup==="username is already exist") { %>
    <div class="d-flex justify-content-center m-5 card-img-overlay" style="max-height: 13%; color: red;">
        <div class="toast toast-demo fade show" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-header">
            <strong class="me-auto">แจ้งเตือน</strong>
            <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body">
                Username หรือ Email ซ้ำกรุณากรอกใหม่
            </div>
        </div>
    </div>
<% } %>